DATABASE_URL="postgresql://user:password@localhost:5432/geta_construction"
JWT_SECRET="change-this-secret-in-production"
PORT=4000
CORS_ORIGIN="http://localhost:5173"

# Cloudflare R2 (S3-compatible) — used to store transaction attachments
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# Public base URL for the bucket (R2.dev subdomain or your own custom domain), no trailing slash
R2_PUBLIC_URL=
